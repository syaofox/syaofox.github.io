name: Manual Deploy to GitHub Pages

on:
  workflow_dispatch:
    inputs:
      message:
        description: '提交信息'
        required: false
        default: 'Manual update from GitHub Action'
        type: string

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    concurrency: 
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
      
      - name: Set up Python
        run: uv python install
      
      - name: Install dependencies
        run: uv sync
      
      - name: Generate HTML content
        env:
          GITHUB_TOKEN: ${{ secrets.BLOG_SECRET }}
          TZ: Asia/Shanghai  # 设置时区为北京时间
        run: uv run python main.py
      
      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./html
          publish_branch: gh-pages
          force_orphan: true
      
      - name: Commit changes to main
        env:
          USERNAME: ${{ secrets.GIT_USERNAME }}
          EMAIL: ${{ secrets.GIT_EMAIL }}
          GITHUB_TOKEN: ${{ secrets.BLOG_SECRET }}
        run: |
          # 配置 git 信息
          git config --global user.name $USERNAME
          git config --global user.email $EMAIL
          
          # 显示当前状态
          echo "Checking for changes..."
          git status
          
          # 添加所有更改的文件
          git add README.md assets/
          
          # 显示暂存的文件
          echo "Staged files:"
          git diff --staged --name-only
          
          # 检查是否有更改需要提交
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            echo "Committing changes..."
            git commit -m "${{ inputs.message }}"
            
            # 推送更改到 main 分支
            echo "Pushing to main branch..."
            git push https://$USERNAME:$GITHUB_TOKEN@github.com/$GITHUB_REPOSITORY.git main
            echo "Changes pushed to main branch"
          fi
      
      - name: Done
        run: echo 'Manual deployment completed successfully!'